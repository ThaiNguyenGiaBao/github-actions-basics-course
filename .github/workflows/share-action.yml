name: Shared actions

on:
    push:

jobs:
    checkout-repo:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v3
            - run: ls 
            - run: echo "Hello from the shared action"
            - run: mkdir new_folder
            - run: ls